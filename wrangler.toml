name = "sloppify"
compatibility_date = "2024-11-01"
pages_build_output_dir = "."

# Production bindings are configured in the Cloudflare Pages dashboard
# (Settings -> Functions -> KV namespace bindings, and Settings -> Environment variables).
#
# For local `wrangler pages dev`, create a KV namespace and uncomment:
# [[kv_namespaces]]
# binding = "RL"
# id = "your-kv-namespace-id"
