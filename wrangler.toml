name = "sloppify"
compatibility_date = "2024-11-01"
pages_build_output_dir = "."

# Local `wrangler pages dev` picks this up automatically.
# For production: Cloudflare Pages → Settings → Functions → KV bindings → bind as RL
# (same namespace id), plus set ANTHROPIC_API_KEY as a secret.

[[kv_namespaces]]
binding = "RL"
id = "f340d2031a0e4fafac426da857c78197"
